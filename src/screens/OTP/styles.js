import Colors from "_styles/colors";
import {
  fixedHeight,
  fixedWidth,
  STATUSBAR_HEIGHT,
  ScreenHeight,
  ScreenWidth,
} from "_styles/dimension";
//// This Stylesheet belongs to  OTP
export default styles = {
  blackText: {
    textAlign: "center",
    fontSize: ScreenWidth * 0.0476,
    fontWeight: "600",
    color: Colors.black,
  },
};
